import React from 'react';

const CTA = () => (
    <div className="cta">
        <h1 className="cta__heading">Find out more about how we work</h1>
        <button className="btn btn--gray">How we work</button>
    </div>
)

export default CTA;